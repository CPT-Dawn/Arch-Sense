[Unit]
Description=Arch-Sense — Apply saved keyboard RGB settings
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/usr/bin/arch-sense --apply
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target